export enum Group_Types {
    PHYSICAL = 'physical',
    VIRTUAL = 'virtual',
}

export enum GROUP_STATUS {
    PENDING = 'pending',
    ACTIVE = 'active',
    INACTIVE = 'inActive',
    REJECTED = 'rejected',
}

export enum GROUP_TARGET_AUDIENCE {
    MALE = 'male',
    FEMALE = 'female',
    KIDS = 'kids',
}
